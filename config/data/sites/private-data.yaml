# This is all the private data that should not not leak into the standard repo
# We set .gitignore for that aka private-data.yaml is ignored.

general:
    host: 35.184.136.113:8443
    baseUrl: "https://${general.host}"
    credentials:
        username: 
        password: