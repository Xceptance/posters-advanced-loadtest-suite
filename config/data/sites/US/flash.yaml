# I am holding the flash definition
flash:
    # in seconds
    refreshTime: 10
    # from beginning until we want to start the flash, in min
    startTime: 0
    # OCPI use not this url but a direct product id
    product:
        urls:
            - ${general.baseUrl}/s/RefArch/pleated-jacket/25697499M.html?lang=en_US
            - ${general.baseUrl}/s/RefArch/electronics/televisions/flat screen/sony-kdl-52s4100M.html?lang=en_US
    # This need to be changed here, always as string to avoid side effects 
    ocapi_product:
        id:
            - "Number"